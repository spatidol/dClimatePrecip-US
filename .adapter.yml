name: dClimate Precipitation US
description: Request a us location and date - lat, lon, unix timestamp - to get historical precipitation data for that day in mm.

node_identifier: DCP

platforms:
  - Docker

data_sources:
  - name: dClimate
    url: https://api.dclimate.net/

parameters:
  - lat
  - lon
  - date
